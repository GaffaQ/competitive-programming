#include <bits/stdc++.h>
#define ll long long;

using namespace std;

int main () {

    int n,q;
    cin >> n >> q;
    vector<int> arr(n);

    for(auto &a : arr) a = 0;

    for (int i = 0; i < q; i++) {
        int t,x,y; cin >> t >> x >> y;
        if(t % 2 == 0){
            for () {
                
            }
        }else{

        }
    }

}