#include <bits/stdc++.h>

using namespace std;

int main () {

    int t; cin >> t;
    while(t--){
        int n; cin >> n;
        int m,k;

        int arr[n];
        m = k = n;
        while (m--) cin >> arr[n]; 
        sort(arr, arr+n);

        int der = 0;
        while (k--) {
            if () 
            der += arr[n];

        }

    }

}