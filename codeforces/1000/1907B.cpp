#include <bits/stdc++.h>
#define bismillah ios::sync_with_stdio(false); cin.tie(NULL);
#define ll long long
#define endl '\n'

using namespace std;

int main () {
    bismillah
    
    stack<int> st;
    st.push(1);
    st.push(1);
    st.push(1);
    st.push(1);
    int x = st.top();
    x = st.top();
    x = st.top();
    cout << x << endl;
    
}